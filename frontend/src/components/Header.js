import React from 'react'

const Header = () => { // NavBar needs sized logo, Summer title, and various buttons (sign in is styled uniquely)
  return (

        <div></div>

    )
}

export default Header