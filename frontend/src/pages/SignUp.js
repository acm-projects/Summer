import React from 'react'

const Signup = () => {
	return (
		// <div className="page-container">
		// 	<div className="form">
		// 		<div className="subtitle">
		// 			<img src={logo} alt='logo' />
		// 			<p>Summer</p>
		// 		</div>

		// 		<div className='cover'>
		// 			<p>Login</p>

		// 			{/* Calling to the methods */}
		// 			<div className="messages">
		// 				{errorMessage()}
		// 				{successMessage()}
		// 			</div>

		// 			<form>
		// 				{/* Labels and inputs for form data */}
		// 				<input onChange={handleName} className='log-input'
		// 					value={name} type="text" placeholder='Name' />

		// 				<input onChange={handleEmail} className="log-input"
		// 					value={email} type="email" placeholder='Email' />

		// 				<input onChange={handlePassword} className="log-input"
		// 					value={password} type="password" placeholder='Password' />

		// 				<button onClick={handleSubmit} className="btn" type="submit">
		// 					<span>LOG IN</span>
		// 				</button>

		// 				<div className='horizontal-line'></div>
		// 			</form>


		// 			<div className='signup-prompt'>
		// 				<div>Don't have an account?</div>
		// 				<Link to='/signup' className='signup-text'>Sign Up</Link>
		// 			</div>

		// 		</div>
		// 	</div>
		// </div>

		<div>
			Sign up 
		</div>
	)
}

export default Signup